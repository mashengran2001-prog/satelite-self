# npmmirror — npm registry mirror for faster installs in China.
registry=https://registry.npmmirror.com/

# Only resolve optional deps for the current OS/CPU. pnpm otherwise
# fetches every platform binary listed in the lockfile (e.g. linux-s390x).
supported-architectures.os[]=current
supported-architectures.cpu[]=current
